// Shared by the window and home tab to avoid circular imports.
// Calories use kcal; protein, carbohydrates, and fat use grams.
export struct MacroValues {
    kcal: float,
    proteins: float,
    carbohydrates: float,
    fat: float,
}

